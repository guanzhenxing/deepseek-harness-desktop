{
  "name": "deepseek-harness-desktop",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "tsc -b",
    "check": "pnpm run format:check && pnpm run lint && pnpm run typecheck && pnpm run test:unit && pnpm run check:docs",
    "check:boundaries": "node scripts/verify-boundaries.mjs",
    "check:docs": "node scripts/verify-docs.mjs",
    "format:check": "prettier --check eslint.config.js vitest.config.ts vitest.integration.config.ts docs/compatibility.json scripts/*.mjs tests/smoke/*.mjs \"{apps,packages}/**/*.{ts,json}\" --ignore-unknown --no-error-on-unmatched-pattern",
    "lint": "eslint . && pnpm run check:boundaries",
    "smoke:dsh-ui": "node tests/smoke/dsh-ui.mjs",
    "smoke:host-crash": "node tests/smoke/host-crash.mjs",
    "test:integration": "pnpm run build && vitest run --config vitest.integration.config.ts",
    "test:unit": "node --test scripts/verify-boundaries.test.mjs && vitest run",
    "typecheck": "tsc -b --force --pretty false"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.11.1",
    "eslint": "10.9.1",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "typescript-eslint": "8.69.0",
    "vite-tsconfig-paths": "6.1.1",
    "vitest": "4.1.11"
  }
}
